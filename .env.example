# FastAPI
HOST=0.0.0.0
PORT=8000

# Supabase Postgres (recommended: use Supabase "Connection string" -> "URI")
# Example: postgresql://postgres:<password>@db.<ref>.supabase.co:5432/postgres
SUPABASE_DB_URL=

# OpenAI (optional but recommended for real LLM streaming + summarization)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
